---
# Deploy a single AAP instance
# Variables expected:
#   _instance_name: Name for this AAP instance
#   _instance_namespace: Namespace for this instance

- name: Generate deterministic admin password if not provided
  when: aap_instance_admin_password | default('') | length == 0
  ansible.builtin.set_fact:
    _aap_instance_admin_password: "{{ ((aap_instance_password_salt ~ '-' ~ _instance_namespace) | hash('sha256'))[:aap_instance_admin_password_length] }}"

- name: Use provided admin password
  when: aap_instance_admin_password | default('') | length > 0
  ansible.builtin.set_fact:
    _aap_instance_admin_password: "{{ aap_instance_admin_password }}"

- name: Create admin secret for AAP instance
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: "{{ _instance_name }}-admin-password"
        namespace: "{{ _instance_namespace }}"
      type: Opaque
      stringData:
        password: "{{ _aap_instance_admin_password }}"
  no_log: "{{ aap_instance_no_log }}"

- name: Deploy AAP instance
  kubernetes.core.k8s:
    state: present
    template: aap_instance.yaml.j2
  vars:
    instance_name: "{{ _instance_name }}"
    instance_namespace: "{{ _instance_namespace }}"

- name: Wait for AAP instance to be ready
  kubernetes.core.k8s_info:
    api_version: aap.ansible.com/v1alpha1
    kind: AnsibleAutomationPlatform
    name: "{{ _instance_name }}"
    namespace: "{{ _instance_namespace }}"
    wait: true
    wait_condition:
      type: Successful
      reason: Successful
      status: "True"
    wait_timeout: 1800
  register: r_aap_instance

- name: Get routes for AAP instance
  kubernetes.core.k8s_info:
    api_version: route.openshift.io/v1
    kind: Route
    namespace: "{{ _instance_namespace }}"
  register: r_instance_routes

- name: Extract instance URLs
  ansible.builtin.set_fact:
    # AAP 2.6: Extract both gateway route (for OAuth/tokens) and controller route (for manifest/controller API)
    # Gateway route name matches the AAP instance name (without -controller suffix)
    # Controller route name has -controller suffix
    _gateway_url: "https://{{ (r_instance_routes.resources | selectattr('metadata.name', 'equalto', _instance_name) | list | first).spec.host }}"
    _controller_url: "https://{{ (r_instance_routes.resources | selectattr('spec.to.name', 'search', 'controller') | list | first).spec.host }}"
    _controller_hostname: "{{ (r_instance_routes.resources | selectattr('spec.to.name', 'search', 'controller') | list | first).spec.host }}"

# -------------------------------------------------------------------
# Create self-service portal namespace
# -------------------------------------------------------------------
- name: Create self-service portal namespace
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Namespace
      metadata:
        name: "{{ _instance_namespace }}-ssap"
        labels:
          app: self-service-portal
          aap-namespace: "{{ _instance_namespace }}"

- name: Inject manifest into AAP instance
  when:
    - aap_instance_inject_manifest | bool
    - aap_instance_enable_controller | bool
  ansible.builtin.include_tasks: inject_manifest.yml
  vars:
    # AAP 2.6: Use gateway route for health check, controller route for manifest injection
    manifest_gateway_url: "{{ _gateway_url }}"
    manifest_controller_url: "{{ _controller_url }}"
    manifest_admin_password: "{{ _aap_instance_admin_password }}"

- name: Create EDA cluster rolebinding (if enabled)
  when:
    - aap_instance_enable_eda | bool
    - aap_instance_eda_create_rolebinding | bool
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: rbac.authorization.k8s.io/v1
      kind: ClusterRoleBinding
      metadata:
        name: "{{ _instance_name }}-eda-{{ aap_instance_eda_cluster_role }}"
      roleRef:
        apiGroup: rbac.authorization.k8s.io
        kind: ClusterRole
        name: "{{ aap_instance_eda_cluster_role }}"
      subjects:
        - kind: ServiceAccount
          name: "{{ aap_instance_eda_service_account }}"
          namespace: "{{ _instance_namespace }}"

- name: Print instance access information
  ansible.builtin.debug:
    msg:
      - "AAP Instance '{{ _instance_name }}' deployed successfully"
      - "Namespace: {{ _instance_namespace }}"
      - "Controller URL: {{ _controller_url }}"
      - "Admin username: {{ aap_instance_admin_username }}"
      - "Admin password: {{ _aap_instance_admin_password }}"
      - ""
      - "Components enabled:"
      - "  Controller: {{ aap_instance_enable_controller }}"
      - "  EDA: {{ aap_instance_enable_eda }}"
      - "  Hub: {{ aap_instance_enable_hub }}"
      - "  Lightspeed: {{ aap_instance_enable_lightspeed }}"

- name: Save instance information to fact
  ansible.builtin.set_fact:
    aap_instance_info:
      name: "{{ _instance_name }}"
      namespace: "{{ _instance_namespace }}"
      controller_url: "{{ _controller_url }}"
      admin_username: "{{ aap_instance_admin_username }}"
      admin_password: "{{ _aap_instance_admin_password }}"
